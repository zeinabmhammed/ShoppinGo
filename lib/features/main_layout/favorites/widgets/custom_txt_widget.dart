import 'package:flutter/material.dart';

class CustomTxtWidget extends StatelessWidget {
  const CustomTxtWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
